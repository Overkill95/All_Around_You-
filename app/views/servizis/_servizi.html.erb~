<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
<style>
.my-btn {
    background-color:transparent; /* Blue background */
    border: none; /* Remove borders */
    color: #ff4d4d; /* White text */
    padding: 12px 16px; /* Some padding */
    font-size: 16px; /* Set a font size */
    cursor: pointer; /* Mouse pointer on hover */
}

/* Darker background on mouse-over */
.my-btn:hover {
    background-color: #990000;
}
</style>






<li id="Servizi-<%= servizi.id %>">
  <span class="nome"><%= link_to servizi.nome, servizi_path(servizi.id)%></span>
  <span class="indirizzo"><%= servizi.indirizzo %>
  <span class="tipo"><%= servizi.tipo %></span>
  <span class="content"><%= servizi.descrizione %>

  </span>
  <span class="timestamp">
    Creato: <%= time_ago_in_words(servizi.created_at) %> ago.</span>
  <span>
    <% if currentuser==servizi.user %>
      <%= button_to "X", cancellaservizi_path(servizi), class: 'my-btn', method: :get  %>
    <% end %>
  </span>
</li>

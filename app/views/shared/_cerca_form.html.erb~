<style>
input[type=text], select {
    width: 100%;
    padding: 12px 20px;
    background-color: #3CBC8D;
    color: white;
    margin: 8px 0;
    display: inline-block;
    border: 1px solid #ccc;
    border-radius: 4px;
    box-sizing: border-box;
}
input[type=text]:focus {
    background-color: lightblue;
}
.cerca{
   -webkit-transition: width 0.4s ease-in-out;
    transition: width 0.4s ease-in-out;
}
.cerca:focus{
	 width: 100%;
}
div {
    border-radius: 5px;
    padding: 20px;
}
</style>


<div>
<%= form_for (@ricerca,html: { multipart: true }) do |f| %>
    <%= f.text_area :testotipo, placeholder: "Cerca il servizio in base al tipo", class: 'cerca' %>
    <br>
    <br>
    <%= f.text_area :testonome, placeholder: "Cerca il servizio in base al nome", class: 'cerca' %>
    <br><br>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
</div>
